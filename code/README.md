Code (functions, scripts needed for analyses) goes in this folder.

## `statcast.py`
Install [pybaseball](https://github.com/jldbc/pybaseball) with pip:
```
pip install pypaseball
```
Run with python:
```
python statcast.py
```
It will save a csv file to the `/raw-data` directory.
